{
  "name": "paai-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Local dev sandbox for the Post All About It pipeline (Airtable -> Gemini -> 360dialog). Mirrors the Make.com scenario as runnable code, mocked by default.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
